TOT=0
ARG=0
i=0
max=120
n=100

while [ $i -lt $max ]
do
	TOT+=$(echo " + $(ARG=$(./rnd -100000 100000 $n) && ./push_swap $ARG | wc -l | bc)")
	echo "i = $i"
#Total is TOT = $TOT"
	sleep 1
	i=$[$i+1]
done

TOT=$(echo "$(echo $TOT | bc) / $max" | bc)
echo "Average for n = $n is $TOT"